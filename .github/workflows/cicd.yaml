name: Build, Push, and Deploy to Server (Self-Hosted Rollback Ready)

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: self-hosted

    steps:
      # 1️⃣ Use local source code instead of checkout
      - name: Use local source code
        run: |
          cd /home/mladmin/zeek_xgboot_ml
          echo "📂 Using existing local source code from server"
          pwd
          ls -la

      # 2️⃣Login Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 3️⃣ Set version variables
      - name: Set version variables
        id: vars
        run: |
          VERSION="1.0.${{ github.run_number }}"
          BRANCH="main"
          COMMIT_SHA=$(echo $GITHUB_SHA | cut -c1-6)
          TAG="${VERSION}-${BRANCH}-${COMMIT_SHA}"

          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "TAG=$TAG" >> $GITHUB_ENV
          echo "🏷️ Tag will be: ${TAG}"

      # 4️⃣ Build Docker image
      - name: Build Docker image
        working-directory: /home/mladmin/zeek_xgboot_ml
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/zeek-prepare-data:${TAG} .

      # 5️⃣ Push Docker image
      - name: Push Docker image
        run: |
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/zeek-prepare-data:${TAG}

      # 6️⃣ Update docker-compose.yaml with new tag
      - name: Update docker-compose.yaml with new tag
        working-directory: /home/mladmin/zeek_xgboot_ml
        run: |
          sed -i "s|supreecha2003/zeek-prepare-data:.*|supreecha2003/zeek-prepare-data:${TAG}|g" docker-compose.yaml
          echo "✅ docker-compose.yaml updated with tag ${TAG}"
          cat docker-compose.yaml

      # 7️⃣ Save compose file as artifact
      - name: Upload compose file artifact
        uses: actions/upload-artifact@v4
        with:
          name: docker-compose-latest
          path: /home/mladmin/zeek_xgboot_ml/docker-compose.yaml

  deploy:
    runs-on: self-hosted
    needs: build  

    steps:
      # 1️⃣ ใช้โค้ดในเครื่อง ไม่ต้อง checkout
      - name: Use local source code
        run: |
          cd /home/mladmin/zeek_xgboot_ml
          echo "📂 Using existing local source code from server"
          pwd
          ls -la
          
      - name: Copy latest model into output
        run: |
          echo "🧠 Copying latest model into output folder..."
          cp /home/mladmin/zeek_xgboot_ml/model_repo/xgboost-model.pkl /home/mladmin/zeek_xgboot_ml/data/output/xgboost-model.pkl
          ls -lh /home/mladmin/zeek_xgboot_ml/data/output/

      # 2️⃣ Download compose artifact จาก job build
      - name: Download docker-compose.yaml artifact
        uses: actions/download-artifact@v4
        with:
          name: docker-compose-latest

      # 3️⃣ Deploy services locally (no SSH)
      - name: Deploy updated containers locally
        run: |
          cd /home/mladmin/zeek_xgboot_ml
          echo "🚀 Pulling new image..."
          docker compose pull
          echo "🔁 Restarting containers..."
          docker compose up -d --force-recreate
          echo "🧹 Cleaning up old images..."
          docker system prune -af
          echo "✅ Deployment complete!"
